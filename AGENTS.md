# Codex 專案總指令

本文件適用於整個 Repository。所有自動化代理、研究者與貢獻者在修改檔案前，必須先閱讀本文件、`PROJECT_SPEC.md`、`ROADMAP.md` 與相關方法文件。

## 專案定位

- 專案名稱：臺北市社區發展協會研究
- Repository：`CSLee2291/taipei-community-research`
- 研究期間：2023–2026
- 第一階段地理範圍：臺北市萬華區
- 主要語言：繁體中文（`zh-TW`）
- 性質：長期、開源、可重製的學術研究與公共知識庫，不只是軟體專案

報告、Dashboard、網站、說明文件與面向使用者的文字必須使用繁體中文。程式識別字、資料欄位、標準名稱與必要的技術錯誤訊息可使用英文。

## 不可違反的研究規則

1. 不得捏造資料、來源、活動、獎項、補助、評分或組織狀態。
2. 每項事實都必須能回溯至可驗證來源；無法驗證者留空並建立 TODO。
3. 不得把「查無公開資料」寫成「沒有活動」或「未營運」。
4. 教學或合成資料必須使用 `is_example=true` 與 `synthetic_example` 標示，且不得進入正式統計、排名或研究結論。
5. AI 僅能協助抽取、分類、檢查與提出候選建議；正式事實、SDG 對應與評分必須有人工作業與來源證據。
6. 不得提交密碼、API Key、私人聯絡資料、未去識別訪談資料或其他敏感資訊。
7. 既有資料與使用者變更不得被無關工作覆寫或刪除。

## 來源優先順序

1. 臺北市政府資料開放平台
2. 臺北市政府社會局
3. 臺北市萬華區公所
4. 衛生福利部
5. 環境部
6. 協會官方網站
7. 協會官方 Facebook

部落格、未具名彙整頁與搜尋結果頁不得作為主要證據。每筆來源至少記錄標題、發布者、網址或穩定識別碼、資料日期、存取日期與適用授權。

## 資料與檔案規則

- 文字與 CSV 一律使用 UTF-8。
- 日期採 ISO 8601；座標採 WGS84。
- `data/external/` 與 `data/raw/` 保存不可變更的來源快照。
- `data/csv/` 與 `data/json/` 是可交換的發布資料層，兩者必須同步。
- `data/processed/` 只放通過驗證、可供分析的衍生資料。
- 使用穩定識別碼；不得因名稱或年度改變而重用或重新編號。
- 正式輸出必須保留資料版本、schema 版本、來源與產生日期。
- 檔名、欄位與版本規則以 `PROJECT_SPEC.md` 和 `docs/data-dictionary.md` 為準。

## 實作與驗證

- Python 自動化以 Python 3.12 為基準。
- 現有 Dashboard 採 Node.js、React、vinext 與 Google Maps；不得為了套用範例技術棧而破壞已驗證功能。
- 地圖應保留 Google Maps 整合；如新增 Leaflet，應作為無金鑰的公開地圖替代方案。
- 所有資料轉換必須能由已提交來源與腳本重製。
- 修改資料時至少執行資料 schema、型別、唯一性、外鍵、CSV／JSON 一致性與合成資料隔離檢查。
- 修改 Dashboard 或網站時，必須執行建置、測試、lint 與基本響應式檢查。
- GitHub Actions 的資料同步、JSON 匯出、報告與 Dashboard 完整重建必須由使用者手動觸發，不設定每日資料更新排程。

## Sprint 與里程碑流程

每次只執行 `docs/sprints/README.md` 中已指定的 Sprint 或明確工作項目，不把未研究的後續 Sprint 假裝成已完成。

當使用者明確要求完成一個里程碑時：

1. 確認 Sprint 目標、來源範圍與驗收條件。
2. 保存來源與 provenance，再進行轉換。
3. 產生或更新 CSV、JSON、Markdown、網站、Dashboard 或報告。
4. 執行與風險相稱的驗證。
5. 更新 `README.md`、`CHANGELOG.md` 與 `ROADMAP.md`。
6. 建立內容明確的 Commit。
7. Push 至 `agent/*` 分支並建立或更新 Pull Request。

若缺少官方資料、授權、認證或必要人工判斷，應建立具體 TODO 並說明阻礙，不得以推測完成欄位。
